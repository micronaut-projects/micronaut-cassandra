pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
    }
}
rootProject.name = 'cassandra-parent'

plugins {
    id("io.micronaut.build.shared.settings") version "5.2.3"
}

include 'cassandra'
